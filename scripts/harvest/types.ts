// Deprecated: legacy TypeScript-only interfaces.
// Re-export Zod inferred types from schemas for backwards compatibility.
export type {
  PartySpell,
  SeatSpell,
  MemberCore,
  NormalizedData,
  Event,
  SnapshotMember,
  Snapshot,
  HarvestConfig,
} from './schemas';

export type ISODate = string;